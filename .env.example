NOCODB_TOKEN=qgM7rqQxsi8OXy8TFdPCgRgwMutL1uTlDff4hCjN
NOCODB_BASE_ID=pae9dl82usu5wvw
NOCODB_URL=http://localhost:8500/api/v3

# Directory containing .pmtiles files served by the webapp
PM_TILES_DIR=./data/export

# Directory where entity images (country flags, team photos, …) are stored.
# Subdirectories: country/, team/  — each contains manifest.json + image files.
# Run `just pipelines export-country-images` / `export-team-images` to populate.
EXPORT_IMAGES_DIR=./data/export/images

# PostgreSQL (used by NocoDB in docker-compose.deploy.yml)
POSTGRES_DB=nocodb
POSTGRES_USER=nocodb
POSTGRES_PASSWORD=changeme
